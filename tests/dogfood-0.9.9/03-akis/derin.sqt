int derin(int n) { if (n <= 0) { return 0; } return 1 + derin(n - 1); }
int main() {
    print(derin(1000)); print(" ");
    print(derin(10000)); print(" ");
    print(derin(100000));
    return 0;
}
